# Vercel Environment Variables Example
# Copy this to .env and fill in your values.
# Also set in Vercel project settings (Settings > Environment Variables).

# Vercel Blob (required for all data storage and bug report screenshots)
# Get this from your Vercel project: Storage > Blob > Connect Store
BLOB_READ_WRITE_TOKEN=
